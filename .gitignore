# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
dist/
.ruff_cache/
.pytest_cache/

# SQLite DB (stays on VM only)
data/s7bb.db
data/s7bb.db-shm
data/s7bb.db-wal

# Environment
.env

# TLS certificates / keys (never commit)
certificate/

# DB Marketplace credentials (never commit)
db-api/client_id
db-api/client_secret

# Node
node_modules/
site/dist/

# OS
.DS_Store

# Claude Code local overrides
.claude/settings.local.json
